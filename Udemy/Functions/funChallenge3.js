/* Write a arrow function names: "calculateTotal" that takes two parameters price & quantity. The func should return the total cost by multiplying the price and quantity. Store the result in variable name "totalCost".*/

const calculateTotal = (Price, Quantity) => "Total cost is: " + Price*Quantity;
let totalCost = calculateTotal(4, 5)
console.log(totalCost);

